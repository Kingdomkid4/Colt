using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enemy : MonoBehaviour
{

    /*double attackTurnTime = 0.7;
    int rotateSpeed = 120;
    int attackDistance = 17;
    int extraRunTime = 2;
    int damage = 1;
    int attackSpeed = 1;
    int attackRotateSpeed = 20;
    double idleTime = 1.6;
    Vector3 puncPosititon = new Vector3((float)0.4, 0, (float)0.7);
    double punchRadius = 1.1;
    AudioClip idleSound;
    AudioClip attackSound;
    private int attackAngle = 10;
    private bool isAttacking = false;
    private int lastPunchTime = 0;
    Transform target;
    private CharacterController characterController;

    // Use this for initialization
    void Start ()
    {
        characterController.GetComponent<CharacterController>();

        if (!target)
        {
            target = GameObject.FindGameObjectWithTag("Player").transform;
            GetComponent<Animation>().wrapMode = WrapMode.Loop;
            GetComponent<Animation>().Stop();
        }

        GetComponent<AudioSource>().clip = idleSound;
        //wait for random # of seconds
        while (true)
        {
            yield Idle();
            yield Attack();
        }
    }
	
	// Update is called once per frame
	void Update ()
    {
		
	}*/
}
